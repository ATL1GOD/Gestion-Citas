/**
 * Representa una especialidad médica (ej. Cardiología).
 * El ID se llama 'idEspecialidad' en el backend.
 * Archivo de origen: DoctorController.java, EspecialidadController.java
 */
export interface Especialidad {
  idEspecialidad: number;
  nombre: string;
}